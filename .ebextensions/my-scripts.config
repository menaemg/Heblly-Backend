container_commands:
    01-migration:
        command: "php artisan --env=staging migrate"
        leader_only: true
    02-migration:
        command: "composer install --optimize-autoloader --no-dev"
        leader_only: true
